{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "minimumReleaseAge": "14 days",
  "internalChecksFilter": "strict",
  "customManagers": [
    {
      "description": "Keep the SHA-pinned `uses:` examples in index.html current, the same way buildcage/docker tracks the ones in its Markdown docs. The examples are syntax-highlighted, so `(?:</?[a-z][^>]*>|\\s)*` lets tags and whitespace sit between the tokens; it still matches the plain-text form too.",
      "customType": "regex",
      "managerFilePatterns": ["/\\.html$/"],
      "matchStrings": [
        "uses:(?:</?[a-z][^>]*>|\\s)*(?<depName>[a-zA-Z0-9_.-]+/[a-zA-Z0-9_.-]+)(?:/[^@\\s<]+)?@(?<currentDigest>[a-f0-9]{40})(?:</?[a-z][^>]*>|\\s)*#\\s*(?<currentValue>v[0-9.]+)"
      ],
      "datasourceTemplate": "github-tags",
      "versioningTemplate": "semver"
    }
  ],
  "packageRules": [
    {
      "description": "Buildcage's own actions are exempt from minimumReleaseAge, so the landing page can show a new release immediately.",
      "matchPackageNames": ["/^buildcage\\//"],
      "minimumReleaseAge": "0 days"
    }
  ]
}
